---
import { whatsappLink } from '../data/site';
import WhatsAppIcon from './WhatsAppIcon.astro';

interface Props {
  title: string;
  subtitle: string;
  ctaSecondary?: { label: string; href: string };
}

const { title, subtitle, ctaSecondary = { label: 'See rates', href: '/pricing' } } = Astro.props;
---

<section class="page-x section-y">
  <div class="grid items-center gap-8 lg:grid-cols-2 lg:gap-12">
    <div>
      <p class="eyebrow">Santa Teresa, Costa Rica</p>
      <h1 class="mt-3 text-h2 sm:text-h1">{title}</h1>
      <p class="mt-4 max-w-xl text-lead text-muted">{subtitle}</p>

      <div class="mt-8 flex flex-col gap-3 sm:flex-row sm:items-center">
        <a href={whatsappLink()} class="btn-primary" rel="noopener" target="_blank">
          <WhatsAppIcon class="h-4 w-4" />
          Check availability
        </a>
        <a href={ctaSecondary.href} class="btn-secondary">{ctaSecondary.label}</a>
      </div>

      <p class="mt-4 text-body text-muted">
        Same-day pickup · Free hotel delivery in Santa Teresa
      </p>
    </div>

    <!-- Floating product shot. No frame or fill behind it: the PNG alpha does
         the work, and a soft contact shadow keeps it from looking pasted on. -->
    <div class="flex items-center justify-center">
      <img
        src="/atv-hero.webp"
        srcset="/atv-hero-700.webp 700w, /atv-hero.webp 1400w"
        sizes="(min-width: 1024px) 50vw, 100vw"
        alt="Red Honda Rancher ATV with front and rear cargo racks, available to rent in Santa Teresa"
        width="1400"
        height="919"
        loading="eager"
        fetchpriority="high"
        decoding="async"
        class="h-auto w-full max-w-xl [filter:drop-shadow(0_28px_28px_rgba(0,0,0,0.20))]"
      />
    </div>
  </div>
</section>
